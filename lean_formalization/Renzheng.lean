import Renzheng.Core

/-!
# 论文数学命题的 Lean 4 形式化核验（入口文件）

全部内容见 `Renzheng/Core.lean`。构建方式：

```
lake build          # 或在项目根目录执行：lake env lean Renzheng/Core.lean
```

依赖：Lean v4.33.0 + Mathlib（`lean-toolchain` 与 `lakefile.toml` 已固定版本）。
-/
